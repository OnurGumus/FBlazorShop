{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Server",
            "command": "dotnet",
            "args": [ "build" ],
            "type": "shell",
            "options": { "cwd": "${workspaceFolder}/FBlazorShop.Web" },
            "group": "build",
            "problemMatcher": [ "$msCompile" ]
        }
    ]
}

